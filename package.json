{
  "name": "@adalfi/coordinator-core",
  "version": "0.1.0",
  "private": true,
  "description": "Manage DS Components — Phase 1 Coordinator core. Runtime-neutral deterministic engine: curated-source ingestion, derived SQLite index, zero-model resolver, closed contracts, validation and rendering. No model adapter, no Figma access, no plugin packaging.",
  "type": "module",
  "engines": {
    "node": ">=22.18.0"
  },
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "node --test --test-reporter=spec \"tests/**/*.test.ts\"",
    "verify": "npm run typecheck && npm run lint && npm run build && npm test"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "@types/node": "^22.20.1",
    "eslint": "^9.0.0",
    "typescript": "^5.9.0",
    "typescript-eslint": "^8.0.0"
  }
}
